/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: 'at.austrianskills.taskgenerator',
  appName: 'AS TaskGen',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
}

module.exports = config
